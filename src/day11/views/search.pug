extends layout
include mixins/movie

block content
  form(method="GET")
    input(placeholder="Title", type="text", name="keyword", required)
    input(type="submit", value="Search")

  each movie in movies 
    +movie(movie)
  else
    li sorry nothing found
